---
name: play-review-analysis
description: 把几个月的 Google Play 评论熬成「可复现的 bug 知识库」：分类打分、聚合成问题档案、算机型/芯片/版本集中度反推复现坐标，产出 6 张表的 Google Sheets，人工填的状态与复现结论能跨月存活。用在这些场景：「评论分析」「差评分类」「体征分析」「用户反馈太笼统复现不了」「从评论里挖 bug」「把评论变成复现步骤」「问题档案」「评论知识库」「给另一个 app 也建一套」「历史 bug 接进来」「这个问题这期有没有变多」，或用户给出 Play Console 导出的评论 CSV（reviews_reviews_*.csv）想从中定位问题。⚠ 边界：这是【月度沉淀】不是【每日巡检】——如果用户要的是"今天新增了哪些差评""推送到 Telegram""生成差评日报""自动回复差评"，那是 play-console-review-triage / play-store-review-watch 的活，不要用本 skill。本 skill 回答的是"这个问题到底怎么复现"，不是"今天出了什么事"。
---

# Play 评论 → 可复现 bug 知识库

## 这个 skill 解决什么

测试看 Play 评论想找 bug，最常见的挫败是：**"视频播放卡顿"这种评论，没法复现，就没法定位，问题就一直挂着。**

但实测下来，这个判断不完全成立。在 XPlayer 的 5 个月数据里（35734 条评论），逐条人工核对后发现：

> 真正"确有问题但完全无从下手"的，只占有效低星评论的 **2.3%**。
> 绝大多数评论其实带着线索，只是**埋在 40 种语言的自然表达里，关键词挖不出来**。

所以这个 skill 的核心不是"分类评论"，而是**把线索挖出来并让它们累积**：

- 单条笼统评论 = 一个没有坐标的信号点
- 同症状评论叠加后，**元数据（机型/芯片/版本/地区）的交集就是复现坐标**
- 实测效果：「快进后卡顿」在 realme 上集中 **8.7 倍**；「记不住播放进度」7 条证据里 6 条是骁龙机型（**3.2 倍**）——按机型看毫无规律，按芯片族看立刻收敛

## 用户需要提供什么

| | 内容 | 从哪来 | 必需 |
|---|---|---|---|
| 1 | **评论 CSV** | Play Console → 下载报告 → 评论 → 按月下载（UTF-16 编码） | ✅ |
| 2 | **该 app 的分类体系** | 用户现有的一级/二级分类表 | 建议，没有可代为归纳 |
| 3 | 设备目录 CSV | Play Console → 设备目录 → 导出 | 可选，公共资源已有 |
| 4 | 历史 bug 导出 | 缺陷系统导出 CSV | 可选 |
| 5 | 实体测试机资产 | 公司资产台账 | 可选，公共资源已有 |
| 6 | 该 app 专属测试资源目录 | 手上现成的测试文件清单（视频/音频/图片等） | 可选，没有就只给抽象步骤，不点名具体文件 |
| 7 | 该 app 的测试用例文件 | 测试团队自己维护的用例表（xlsx/csv，覆盖功能模块+入口+存储位置+权限+已知错误文案） | 可选，见下方"触发面参考" |

**只有第 1 项是硬门槛。**

**第 7 项：测试用例能显著提升复现路径的准确度，第一次拿到手就要提炼沉淀，别每次现翻。**
用例表里通常藏着三类生成复现步骤时特别缺的信息，而且是"官方定义"的，比临时推演准：
- **入口矩阵**：一个页面/功能可能被几种方式触发（内部列表点开 / 第三方分享唤起 / 在线链接 /
  投屏……），用例表几乎都会按入口分别列用例，比自己现场推演全、准
- **存储位置矩阵**：本地/SD卡/OTG/加密/在线，几乎每个功能点的标准测试矩阵都在重复这几项，
  直接照抄这个矩阵定义"文件从哪来"这个变量，不用自己现编
- **已知薄弱环节**：用例表里"备注"栏/"历史bug"栏经常直接点出"这条路径 fail"、"bug转用例"，
  这是测试团队已经确认过的脆弱点，复现步骤应该优先覆盖这些
- **精确错误文案**：不同故障分支往往有不同的 toast/弹窗文案，证据原文如果提到具体文案关键词
  （哪怕是被翻译过的），可以直接对照用例表里列出的文案反查是哪条代码路径，比"环境信息不足"这种
  笼统结论精确得多

读完提炼成一份项目级参考文档（比如 `trigger-surface.md`，格式参考 XPlayer 项目里已有的那份：
入口分类表 + 存储位置矩阵表 + 权限模型说明 + 已知错误文案表 + 已知薄弱环节列表），**这份文档
不是一次性用完即弃，是要长期维护、每次生成复现步骤都先查它的**——跟 [[repro-steps-generate-not-store]]
"复现步骤本身不入库"不矛盾，这份存的是 app 本身相对稳定的结构性事实（入口/权限模型/错误文案），
不是某次证据推出的具体步骤，性质上更接近 `gotchas.md`。用例随版本迭代会过时，发现和实测对不上
时以实测为准，顺手回来更新这份文档。 用户没有现成分类体系时，读 300 条低星评论归纳一版初稿给用户改——不要凭空设计，要从真实数据里长出来。

依赖：`gspread google-auth`，以及 `~/.claude/credentials/google-workspace/` 下的凭证（见 google-workspace skill）。
公共设备目录在 `~/.claude/shared/device-catalog/`（Play 设备代号→机型/SoC），
公共实体测试机资产在 `~/.claude/shared/test-phones/`（团队手头哪些机器可验证、在谁手上）——
两者都是跨 app 共用的公司级资源，缺失时脚本仍可跑，只是复现路径没法落到"具体哪台机器"。
第 6 项（测试资源目录）是每个 app 各自维护，因为不同 app 需要的测试文件完全不同
（视频播放器需要编码/字幕样本，录音 app 需要音频样本），存在 `<project>/data/test_resources/` 里。

## 快速开始

```bash
SKILL=~/.claude/skills/play-review-analysis

# 1. 初始化项目（建 kb_config.json / taxonomy.py / raw / data）
mkdir -p ~/Projects/myapp-reviews && cd ~/Projects/myapp-reviews
python3 $SKILL/scripts/init_project.py --app MyApp --package com.example.myapp

# 2. 把评论 CSV 放进 raw/，然后按该 app 改 taxonomy.py 的业务域分类

# 3. 跑完整流水线（首次会自动新建 Google Sheets 并把 ID 写回 kb_config.json）
python3 $SKILL/scripts/run_all.py

# 4. 首次额外跑两个（写表3/4/5 的表头 + 格式化）
python3 $SKILL/scripts/init_tabs.py && python3 $SKILL/scripts/format_sheets.py
```

以后每月只需：把新 CSV 丢进 `raw/` → `python3 $SKILL/scripts/run_all.py`。

## 产出的 6 张表

| 表 | 内容 | 谁维护 |
|---|---|---|
| 1_评论库 | 评论原文+机型/SoC/形态+多标签分类+置信度+线索标签 | 脚本 |
| 2_问题档案 | 问题实体：状态/主次证据/伴随度/四维集中度 **（不含复现步骤）** | 脚本生成，人改状态 |
| 3_复现尝试记录 | 谁 用什么机型 试了什么 结果 **★排除了什么** | 人工 |
| 4_历史BUG | 缺陷系统导出后映射进来 | 导入 |
| 5_分类词典 | 词表在线可读 | 脚本 |
| 6_月度趋势 | 版本×分类的密度，发现回归 | 脚本 |

## 表格怎么刷新（最容易踩的坑）

**没有任何自动更新。** 评论库变了，问题档案和月度趋势不会自己跟着变——三张表都是脚本
生成的静态数据，必须重跑 `run_all.py` 一起刷新。

### 依赖顺序不能乱

```
classify.py  ──> build_issues.py ──> export_sheets.py ──> build_trends.py ──> upload.py
分类评论          聚合问题档案         生成评论库           算趋势              上传
                  ↓ 产出 ID 映射       ↑ 回填「关联问题ID」
                  review_to_issue.json
```

`build_issues` 必须跑在 `export_sheets` **之前**——后者要用前者产出的 ID 映射回填「关联问题ID」列。
顺序反了这一列就全空。`run_all.py` 已经固化了顺序，直接用它就不会错。

### 哪些会被覆盖，哪些被保护

| 表 | 刷新方式 | 人工内容 |
|---|---|---|
| 1_评论库 | **整表重建** | ⚠ 无保护。改分类要写 `data/manual_override.json`，别直接改表格 |
| 2_问题档案 | 重建，但**先读回人工列再合并** | ✅ 状态（4 个人工专属值）/ 关联历史BUG / 负责人 / 备注 |
| 6_月度趋势 | 整表重建 | 无人工内容 |
| 3/4/5 | **不动** | 完全人工维护 |

表2 的保护规则：状态只保留 `可复现-已提单`/`已修复待验证`/`已关闭`/`无法复现-挂起` 这四个——
脚本永远不会自动生成它们，所以出现即说明是人改的。其余状态每次按当期证据重算。
「关联历史BUG」「负责人」「备注」非空即保留。「系统提示」是脚本写的，每次刷新，别往里填。

### 为什么这套保护是必需的

两件事必须同时成立，缺一个知识库就废了：

1. **问题 ID 稳定**（`data/issue_registry.json`）。ID 若按证据强度排序生成，数据一变就漂。
   实测同一个 ISS-001 在几轮迭代里换过 3 次身份。表2 的状态、负责人、历史BUG 全挂在 ID 上，
   ID 一漂，人工填的内容集体错位。

2. **上传前读回人工列**。真实案例：用户自己往「关联历史BUG」填了 23 条 BUG 编号，
   一次常规刷新差点全部冲掉，靠这个机制救回来的。

**改了表结构（加列/换列序）后要特别小心**：`upload.py` 按表头名动态定位列，不会错位；
但如果你手写脚本按下标取列，加一列就全乱。

### 常见症状对照

| 现象 | 原因 |
|---|---|
| 「关联问题ID」整列空 | `export_sheets` 跑在了 `build_issues` 前面 |
| 中文列显示 `=IFERROR(GOOGLETRANSLATE(...))` | 写入时没用 `USER_ENTERED` |
| 中文列大片空白 | GOOGLETRANSLATE 异步计算，等 30~60 秒 |
| 填的状态/负责人不见了 | 用了旧版 upload，或手写脚本覆盖 |
| 某张表数据明显过时 | 它没被挂进 `run_all.py`。**任何脚本生成的表不进统一流程，必然腐烂** |


## 六个关键设计（照抄容易，理解了才改得动）

### 1. 复现步骤不入库，每次现生成

表2 **故意没有**「复现步骤」列。随证据增长最优步骤会变，存一份旧的反而误导测试往错方向试。

但**复现尝试的结果必须存**（表3）。`★本次排除了什么` 是整个库最值钱的一列——
它让每次新生成的步骤从排除清单**之后**接着走，而不是原地绕圈。

「无法复现」因此不是终点，而是**带着已排除清单的挂起状态**。

### 2. 「是不是问题」与「属于哪个功能域」必须解耦

`quality`/`download`/`music` 这些词在 5★ 好评里命中 40-110 次——用户在**夸**这些功能。
所以 L1 关键词只判功能域，是否在抱怨交给 `complaint.py` 门控。

不解耦的后果实测：全量误判 1814 条 → 解耦后 796 条，**56% 是误报**。

### 3. 4-5★ 只认强信号

否定嵌套会骗过弱信号：`nunca me ha fallado`(从没出过问题)、`gak kecewa`(不失望)、
`без рекламы`(没有广告) 全是好评。所以 star≥4 时只认明确故障词。

**铁律：`complaint.STRONG` 只放故障词，话题词一律归 `L1_KEYWORDS`。**

### 4. 信息密度分（A/B/C/D）按"对复现的贡献"加权

| 信号 | 分 | 为什么值这个分 |
|---|---|---|
| 对照实验 | 30 | 用户替你做完了变量控制，最值钱 |
| 规避手段 | 25 | 直接指向代码分支（"每次要切 HW 到 SW"） |
| 精确数值 | 20 | 可直接写成断言（"下载到 97% 停"） |
| 触发条件 | 15 | 复现步骤的主干 |
| 技术标的/频率/时间锚点 | 各 10 | 缩小测试矩阵 |

**D 只给噪音。** 零线索但真实的反馈（`buffering too much`）是 C——
低密度评论是**计数器**（证明问题存在、测规模），高密度评论是**探针**（提供复现坐标），角色不同都不能丢。

### 5. 多标签 + 主次证据

一条评论报告多个问题时同时计入多个档案，但次证据**权重减半**（多重关键词命中含误报）。

副产品是**伴随度**（次证据占比）：伴随度 ≥70% 的问题几乎从不单独出现，
很可能与主症状同根因。这是单标签统计完全看不到的信号。

### 6. 问题 ID 必须稳定，人工填写必须保护

ID 若按证据强度排序生成，数据一变就漂（实测同一个 ISS-001 换过 3 次身份）。
表2 的状态/负责人/历史BUG 全挂在 ID 上，一漂就集体错位——**这砸的正是知识库的沉淀能力**。

- `data/issue_registry.json` 存 (一级::二级) → ISS-ID 永久映射
- `upload.py` 覆盖前先读回人工列再合并：状态只保留 4 个「人工专属值」
  （可复现-已提单/已修复待验证/已关闭/无法复现-挂起，脚本永不生成这些），
  关联历史BUG/负责人/备注非空即保留

⚠️ **表1 没有人工列保护**，整表重建。修正分类要写 `data/manual_override.json`，别直接改表格。

## 飞行前检查：把踩过的坑变成断言

`run_all.py` 的第一步是 `preflight.py`。设计原则很简单：

> **同一个坑被踩第二次，就说明它不该靠文档提醒，该靠脚本拦。**

这不是理论。真实案例：修 `seka` 子串误伤时我写成 `\bseká?\b`——这是 "sek" + 可选的 "á"，
**匹配不了原词 "seka"**，规则彻底失效却不报错。同一个项目里另一个人独立踩到几乎一样的坑：
`替换?([A-Za-z])` 想让"替换"整体可选，实际只有"换"可选，所有单字母提取全部失败也不报错。

共同点：**量词只作用于紧邻的单个字符/组，写错了不报错，只是永远匹配不上。**
所以 preflight 里最重要的一条检查是——**每条正则至少要能匹配到一条真实评论**。

它还会检查：正则语法、子串误伤（且能与俄语变格这类"故意的词干匹配"区分）、
话题词有没有混进强信号表、L1_PRIORITY 有没有漏分类、CSV 编码、设备目录、未分类率。

有 ERROR 会中止流水线；WARN 只提示。确实要跳过：`--skip-preflight`。

**改完词表一定要跑一次**（`run_all.py` 会自动跑）。注入测试验证过：把修好的 `\bави\b`
改回裸词 `ави`，检查能报出"91 次命中里 89 次落在词中"。

顺带一提，它上线第一次跑就抓到一个我不知道的真问题：`scan` 命中了西班牙语
`bu**scan**do`（寻找）——完全无关的词。


## 三层职责，别混

| 层 | 干什么 | 谁 |
|---|---|---|
| 召回 | 从上万条缩到几百条 | 脚本（**不负责精度**） |
| 抽取 | 症状/触发/环境/对照 四元组，定档，判错的写 override | Claude 精读 |
| 判定 | 复现、回填结果、改状态 | 测试 |

关键词表永远追不上 40 种语言的表达方式。脚本的作用只是把量降到人能读的规模。

## 每月工作流

1. 新 CSV 放进 `raw/` → `run_all.py`
2. **精读两个桶**（不必看全量）：A/B 档（抽四元组、手翻）+ 未分类和笼统桶
3. 判错的写 `data/manual_override.json`；**可泛化的顺手补进 `taxonomy.py`**——
   否则每月都在重犯同样的错，人工量不会下降
4. 改完词表跑 `audit_keywords.py` 自查误伤（用 5★ 好评当阴性对照组）
5. 测试复现完，结果回填表3，**「★本次排除了什么」必须填**

## 生成复现步骤（按需，不存档）

**触发不局限于「生成 ISS-xxx 的复现步骤」这句话本身。** 用户很多时候不会点名 ISS 编号，
而是直接甩过来几条评论原文（可能还带着机型/版本这些零散信息），意图就是「帮我看看这几条
能不能复现」——这也要当成同一个请求主动接住，不要等对方明确说「生成复现步骤」才反应过来。

拿到请求（不管是点名 ISS-xxx，还是直接给了一批评论原文）后，先判断走哪条分支，再走下面
固定顺序——**不能跳步骤，尤其不能跳过"团队手头到底有没有这台机器"这一步**，否则给出的
复现路径测试拿到手根本没法执行。

**0. 分支判断**：
   - 用户点名了 ISS-xxx → 直接进入下面步骤1，走完整流程。
   - 用户直接甩来几条评论原文，没提 ISS 编号 → 先按原文关键片段去表1（`data/sheet1_reviews.json`）
     搜有没有匹配的行（评论内容、评论ID，或者用户附带的评论链接里的 reviewId）。
     - **能匹配上**：读这些行的「关联问题ID」，命中的问题就当作 ISS-xxx 正常走步骤1——
       不要因为用户没报编号就重新分析，已有的问题档案/历史尝试/历史bug 都能直接复用。
       同一批评论如果命中了不止一个问题ID，分别给。
     - **匹配不上**（临时评论、还没跑进知识库的新反馈、或者压根没有这个项目的知识库）：
       这批评论就是全部证据，没有表2/表3/表4 可读，按下面「精简分支」走，**不要假装有完整
       知识库背景**，也不要为了套流程去编一个不存在的问题档案。

**精简分支**（评论匹配不上已有知识库时）：
   1. 直接对这几条评论原文做步骤1里说的"抽取"：症状/触发条件/环境/对照四元组 + 中文翻译；
      评论数量少（通常几条到十几条），信息密度分可以现场估，不用去读表1的密度分列。
   2. 如果评论里带了 Play 设备代号，用 `~/.claude/shared/device-catalog` 翻译成机型/芯片族；
      没带机型信息的评论就明确标"环境信息缺失"，不要编。
   3. **集中度这一步如实说明局限**：几条评论撑不出有效的倍率统计，不能编一个"3.2×"出来。
      如实列出"这几条证据分别在哪些机型/芯片/版本上出现"，如果全部集中在同一个品牌/芯片，
      可以指出"样本虽小但都落在 X 上，值得优先测"，但不能包装成统计意义上的集中度。
   4→7. 后续（查实体机、配测试资源、产出路径、提醒回填）跟步骤4-7完全一样地走，
      只是没有表2状态可改、没有表3历史排除记录可读——路径末尾提醒用户「这批评论不在现有
      知识库项目里，如果后续要跟踪，建议先跑一次 `run_all.py` 把它们正式收进来」。

---

**完整分支**（点名 ISS-xxx，或评论匹配到已有问题档案）：

1. **读证据**：表2 该问题的元数据（状态/主次证据/伴随度）+ 表1 它的全部证据原文
   （★是三层职责里"抽取"那一层已经标好的症状/触发条件/环境/对照四元组，别重新读全量评论）
   + 表3 历史尝试排除了什么 + 表4 相关历史 bug。
   **读证据这一步只要发现任何数据对不上**（最常见：表2「关联评论ID」列出的某个ID在表1里查不到；
   也包括证据条数/主次证据数量跟表2统计字段对不上等）——**第一反应是当场重跑一次 `run_all.py`
   核实，不是猜一个原因写进文档就继续分析**。重跑后原地核对：
   - 缺口消失了 → 是跑分析前数据已经过期，正常，继续往下走即可
   - 缺口依旧存在 → 不是缓存问题，去 `data/classified.json` 按 ID 查这几条的
     `star`/`grade`/`noise`/`primary` 字段定位真实原因（多半是某个脚本的筛选条件跟另一个脚本的
     计数条件对不上），能改的当场改脚本，改完立刻用查到的这几个ID 再跑一次确认解决
   - `export_sheets.py` 跑完会自动做一次「表2 引用的ID 是否都在表1 里」的一致性检查，正常情况下
     根本不该出现这类缺口——如果检查报了错还是选择继续往下分析，等于明知道数据有洞还在洞上面盖楼
   **挑出信息密度分最高的几条（A档优先，B档补充）做成一张表格随复现路径一起输出**：
   原文 + 中文翻译 + 评论链接（表1「评论链接」列）三列都要有，不能只在正文里转述大意——
   测试要能凭这张表直接点进原评论核实，翻译错了也能自己发现。这是硬性要求，不因为
   "已经在正文分析里引用过原文片段"就省略。
2. **算集中度，选维度**：四维集中度（机型/芯片族/版本/地区）哪个倍率最高就按哪个维度定坐标
   （例：「realme 8.7×」就别拿 Pixel 测；「骁龙 3.2×」这种芯片族比具体机型更该优先，
   因为同族芯片常共享编解码器实现，[[shared-device-catalog]] 的 `soc_family()` 用得上）。
3. **翻译代号**：证据里的 Device 列是 Play 设备代号（如 `pa3q`），先用
   `~/.claude/shared/device-catalog` 的 `model_name()` / `soc_family()` 翻成人能看懂的机型/芯片族，
   不要拿代号直接去匹配下一步的资产清单——匹配不上。
4. **查团队手头有没有对应机器（这一步决定路径是否可执行）**：
   ```python
   import sys, os
   sys.path.insert(0, os.path.expanduser('~/.claude/shared/test-phones'))
   from test_phone_lookup import find_candidates, brands_available
   find_candidates(brand='realme')          # 按第 2 步选中的品牌/维度查
   find_candidates(keyword='K50')           # 或按具体机型关键词查
   ```
   只取 `可验证='是'` 的结果（脚本已排除报废/损坏/丢失）。
   **如果全公司台账（`~/.claude/shared/test-phones`）没有匹配，先看项目目录下有没有该测试组自己
   维护的设备清单**（比如 `<project>/data/team_devices/` 下用户提供的组内机型表）——公司级台账
   覆盖广但未必是这个具体小组能马上摸到的机器，组内自己的清单虽然窄，但更贴近"现在就能借到手"的
   范围，值得作为第二顺位查一次，找一到两台接近的补上，而不是查完全公司台账没有就直接跳到云测试
   平台/采购建议。这类清单如果带芯片代号列（不是所有组内清单都有），优先按芯片匹配，没有芯片信息
   的列只能做"品牌+系统版本"弱匹配。
   **两边（全公司台账 + 组内清单）都没有**，才在复现路径里明确写"团队无对应实体机，建议用云测试
   平台/模拟器兜底或申请采购"——不要假装有机器可测，也不要静默换成别的不相关机型。
   拿到候选后从"持有人或地点"里取出联系人/办公地，写进最终路径。
5. **配该 app 的测试资源（技术参数匹配 + 跳出目录判断内容性质）**：
   a. 先按症状关键词（编码/容器/分辨率/HDR/多音轨/字幕格式等技术参数）去
      `<project>/data/test_resources/` 匹配具体文件，点名到文件路径，不要泛泛说
      "用一个 HEVC 视频试试"。
   b. **目录只标了技术参数，没标"内容本身的性质"这个维度——但很多 bug 明显不明显，
      恰恰取决于内容性质而不是编码参数。** 常见几个容易被忽略、需要凭经验判断的维度：
      - 内容类型：动画 vs 真人实拍。动画大色块/锐利边缘/运动矢量简单，真人实拍高频细节/
        连续色彩渐变/复杂运动——同样的编码参数，两者在解码器和渲染管线里的负载完全不同。
        用动画素材复现不明显的卡顿/掉帧/色带问题，换真人电影常常就立刻复现。
      - 运动强度：动作/快速运镜 vs 静态对话。运动补偿、快进/倒放相关的问题需要前者才能触发。
      - 画面复杂度：高频纹理 vs 大片纯色。色带/dithering 类问题需要纯色渐变场景，
        花屏/马赛克类问题需要高频纹理场景。
      - 音轨复杂度：多语言/环绕声混音 vs 单轨立体声。
      **目录里现成文件技术参数对了、但内容性质不对时，不要将就用它凑数**——直接告诉用户
      "目录里现有的 XX 偏动画/静态，建议换成真人电影/高速运动片段测试，原因是……"，
      即使这意味着当下目录里没有现成文件可以点名。这是判断力，不是查表能查到的。
   c. **文件存储位置 + 打开方式，跟内容性质一样容易被忽略，但决定能不能复现**。**先看项目根目录
      有没有 `trigger-surface.md`（从该 app 测试用例提炼的入口/存储位置/权限/错误文案参考，
      见"用户需要提供什么"第7项）——有就直接用它里面的官方入口矩阵和已知薄弱环节，不要凭通用
      安卓知识现场推演；没有就按下面的通用思路判断，且事后如果用户后续提供了测试用例，要回头
      把这两个维度补进新建的 trigger-surface.md**。任何"打开/播放/加载文件"类症状都要过一遍
      这两条：
      - 存储位置：本地内部存储 / 外置 SD 卡（“便携式存储”与“格式化为内部存储”两种模式路径
        体系不同）/ OTG 外接存储 / 云盘自动同步目录。云盘常有"仅在线/智能同步"机制，把不常用
        文件在本地替换成占位 stub，实际数据要点开才补下载——App 若直接按旧路径/MediaStore
        记录读文件，读到的是残缺数据，很容易被误判成"文件损坏"或"格式不兼容"，其实跟编解码
        毫无关系。同一份文件换个存储位置，访问路径、权限模型、I/O 延迟特性完全不同。
      - 打开方式：App 自己扫描媒体库、从内部列表点击播放，走的是 App 完全控制的本地路径；
        vs. 文件躺在别的 App（文件管理器/云盘/聊天软件/同厂商姊妹 App）里，用户走系统分享
        面板或"用其他应用打开"唤起本 App，App 拿到的是外部传入的 `content://` URI，受
        Scoped Storage / SAF 一次性授权约束——是完全不同的代码分支，一个能播不代表另一个
        能播。**证据原文大多数时候不会写清楚是哪种打开方式，两种都要测，不要图省事只测
        "内部库播放"这条最容易操作的路径就交差**。历史 bug 表里如果有"从 XX App 打开视频/
        音频失败"这类记录（哪怕媒体类型不同），直接引用作为这个机制在本 app 上的真实先例，
        写进复现路径里当支撑证据。
   d. 上面 b、c 两条维度如果被反复用到，提醒用户把它们加进目录的"用途标签"体系里
      （见 [[test-resource-catalog-for-repro]]），别每次都临时判断——目录本身也要跟着长。
   e. **证据原文里点名的具体内容，即使目录里没有对应文件，也要单独列出来给测试当备选素材**，
      不能因为"库里没有"就吞掉这条线索。例如证据提到"4K UHD 2160p《蝙蝠侠大战超人》MKV文件"，
      即使目录没有这个文件，也要在复现路径里写出"备选素材：可尝试寻找《蝙蝠侠大战超人》
      4K UHD 2160p MKV 格式文件（证据 Rxxxxx 原文点名），库内暂无对应文件"——具体片名/
      分辨率/封装格式/编码这些细节都要保留，让测试知道去找什么，而不是只给抽象的"4K MKV文件"。
   f. **技术参数在目录里也找不到匹配文件时，主动上网搜开放版权的替代素材**，别止步于"目录没有"
      就交给测试自己想办法。搜索方向：厂商 4K/HDR 演示片（Sony/LG/Samsung 等电视厂商官方
      demo，通常允许自由下载用于演示测试，跟目录里已有的同类文件同一惯例）、CableLabs 4K
      （`cablelabs.com/4k`，明确 CC BY-NC-ND 4.0）、`4kmedia.org` 这类聚合站。
      **禁区（不可协商）**：不能搜索/下载完整版权的院线电影、剧集、音乐等受版权保护的商业作品
      本体——不管证据原文点没点名具体片名，这类内容一律只能停留在"备选素材"文字描述（d 点），
      不能真去找盗版资源满足它。搜到候选后要核实并列出：内容性质是否匹配（真人/动画、
      是否暗场景等，见 5.b）、编码/容器/码率是否匹配症状、授权条款（能不能用于内部测试）、
      下载链接。**下载前必须先把候选列给用户确认**，拿到明确同意再下载——这是下载类动作的
      通用规则，不因为素材开放版权就省略确认这一步。
6. **产出复现路径**：按信息密度分（对照实验 > 规避手段 > 精确数值 > 触发条件 > 技术标的/
   频率/时间锚点）排序输出步骤，每条路径必须包含「在哪台机器（联系谁/哪个办公地）+ **用哪个
   App 版本** + 用什么文件（如果适用）+ 具体操作步骤 + 预期现象」五要素，缺哪个要素就在路径里
   显式留白说明缺什么，不要含糊带过。用完即弃，**不写回表2**。
   **版本怎么选，别漏**：默认推荐"表1 里该问题证据覆盖到的最新版本"（去 `data/sheet1_reviews.json`
   按「关联问题ID」筛出该问题的行，看「应用版本」+「最后更新」，取日期最新那条的版本号）——
   因为如果最新版本已经不复现，说明问题已被顺带修复，没必要拿旧版本较真。
   如果表4 有关联历史 bug 且发现版本明显更老（例如证据集中在 2.x 高版本，历史 bug 发现于
   1.x/2.2.x），额外建议在历史 bug 的发现版本上也测一遍，用来判断"是历史遗留一直没修"还是
   "新版本引入的回归"——这两种结论对开发排查的方向完全不同，路径里要写清楚是哪一种目的。
   证据里的版本号只反映"用户提交评论时的版本"，可能滞后于当前真实最新发布版本，如果差距较大，
   提醒用户核实一下当前 Play 商店在架版本号，不要默认表里最新就是现在最新。
   **默认产出格式是 Markdown 文件**（文件名 `ISS-xxx_复现步骤.md`），聊天里只给摘要不重复整篇内容，
   完整文档写成 .md 用 SendUserFile 发给用户，方便直接转发给团队——不要默认建 Google Doc
   （表格/超链接在 Doc 里排版容易显乱，且多一道创建/共享权限的手续），除非用户明确要 Doc。
   步骤 1 的证据表格、步骤 5.c 的存储位置/打开方式判断、步骤 5.e 的备选素材，
   都是这份 md 里必须包含的小节，不是口头提一句就算。
7. **交给测试做针对性验证，结果回填表3**：验证完必须把结果写回表3，「★本次排除了什么」
   必须填——这是让下一轮生成的路径能接着走、而不是原地绕圈的关键（见"六个关键设计 1"）。

## 参考文件

- `references/gotchas.md` — **改词表前必读**。子串误伤、正则陷阱、Sheets API 坑、
  列号转字母、公式必须 USER_ENTERED 等，全是实际踩出来的
- `references/taxonomy-guide.md` — 怎么为新 app 建分类体系；没有现成体系时如何从数据归纳
- `references/sheet-schema.md` — 6 张表的完整列定义与状态机
- `assets/taxonomy_template.py` — 词表模板，**通用 7 类已内置**，只需补业务域
